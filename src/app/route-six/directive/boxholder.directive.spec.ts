import { BoxholderDirective } from './boxholder.directive';

describe('BoxholderDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxholderDirective();
    expect(directive).toBeTruthy();
  });
});
